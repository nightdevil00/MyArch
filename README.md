# MyArch installation script
installs arch, if the script fails use dos2unix to convert the script
launch with bash ./install_arch.sh
follow the script
enjoy
the other tools are to export installed packages, install fonts etc.
